import React from "react";
import { Input } from "./style";

export const Searchbar = () => {
  return (
    <Input
      type="search"
      id="gsearch"
      name="gsearch"
      placeholder="Search"
    ></Input>
  );
};
